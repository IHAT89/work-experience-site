version: '3'

services:
  work-experience-site:
    container_name: work-experience-site
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:3000"
    environment:
      - NODE_ENV=production
    restart: always
